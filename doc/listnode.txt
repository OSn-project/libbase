To simplify the utilisation of linked lists, libbase has a BListNode class that should be interited by any class 
The structure contains the two fundamental values needed to create a dually-linked list, the pointer to the previous and next node.
It also contains functions for manipulating lists.